# 贡献指南

欢迎补充大湾区温泉区域、温泉小镇、温泉民宿和酒店信息。

## 先记住项目的收录单位

本项目优先收录「温泉区域」，例如镇、村、温泉带或温泉度假区；酒店是区域下的第二层数据。不要把同一片区的每一家酒店都误做成地图区域。

## 提交前检查

```bash
node scripts/validate.js data/regions-gd.json
```

## 数据要求

- 每一条数据必须有来源 `source`；最好同时补充可访问的 `sourceUrl`（当前 schema 允许作为扩展字段）。
- 不从携程、大众点评、美团等平台复制评分、图片或大段文字。
- 价格只能写参考区间，提交前确认它不是已经明显过期的精确价格。
- 坐标优先使用区域/镇级中心点；不确定时保留 `approx: true`，不要伪造精确坐标。
- 无法确认的内容可以提交，但应设置 `verified: false`，并在 `intro` 或 `desc` 中说明待核实。
- 新增酒店时保留 `cert: null`，不要把“真温泉”当作宣传语自行判断。

## 推荐 PR 内容

1. 修改 `data/regions-gd.json`。
2. 在 PR 描述中写明新增/修正了什么、来源链接、核实日期。
3. 运行校验脚本并贴出结果。
4. 如果是关闭、改名或迁址，尽量提供官方公告或门店页面。

数据许可证见 `data/LICENSE`；前端代码许可证见根目录 `LICENSE`。
